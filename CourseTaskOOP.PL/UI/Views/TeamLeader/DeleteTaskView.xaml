<UserControl x:Class="CourseTaskOOP.PL.UI.Views.TeamLeader.DeleteTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CourseTaskOOP.PL.UI.Views.TeamLeader"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Label Content="Tasks"
                   HorizontalContentAlignment="Center"
                   Width="70" />
            <ComboBox Name="TasksComboBox"
                      Width="500"
                      Height="30"
                      Margin="10">
            </ComboBox>
            <Button Name="DeleteTaskButton"
                    Content="Delete task"
                    Width="150"
                    Height="30"
                    Margin="10"
                    Click="DeleteTaskButton_OnClick">
            </Button>
        </StackPanel>
    </Grid>
</UserControl>